<%= form_for :news, url: news_index_path do |f| %>

    <% if @news.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@news.errors.count, "error") %> prohibited
            this article from being saved:
          </h2>
          <ul>
            <% @news.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <p>
      <%= f.label :judul %><br>
      <%= f.text_field :judul %>
    </p>

    <p>
      <%= f.label :isi %><br>
      <%= f.text_area :isi %>
    </p>

    <p>
      <%= f.label :penulis %><br>
      <%= f.text_field :penulis %>
    </p>

    <p>
      <%= f.submit "Simpan Berita"%>
    </p>
<% end %>

<%= link_to 'Back', news_index_path %>